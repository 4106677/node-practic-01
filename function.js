const sum = (num1, num2) => num1 + num2;
const minus = (num1, num2) => num1 - num2;
const multipl = (num1, num2) => num1 * num2;
const divide = (num1, num2) => num1 / num2;

module.exports = {sum,minus,multipl,divide};